'use client';import {useEffect,useState} from 'react';import {apiClient} from '@/services/api';import type {HeroData} from '@/types';export default function Hero(){const [data,setData]=useState<HeroData|null>(null);const [loading,setLoading]=useState(true);useEffect(()=>{apiClient.getHero().then(setData).catch(console.error).finally(()=>setLoading(false));},[]);if(loading)return<section className='min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 flex items-center justify-center'><div className='text-white'>Loading...</div></section>;if(!data)return null;return<section className='min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 text-white py-20 px-4'><div className='max-w-7xl mx-auto grid md:grid-cols-2 gap-12 items-center'><div className='space-y-8'><h1 className='text-5xl font-bold'>{data.headline}</h1><p className='text-xl text-gray-300'>{data.description}</p><div className='flex gap-4'>{data.cta.map((btn,i)=><a key={i} href={btn.href} className='px-6 py-3 rounded-lg font-semibold bg-blue-600 hover:bg-blue-700'>{btn.text}</a>)}</div></div><div className='bg-slate-800 rounded-lg p-8'><h3 className='text-2xl font-bold mb-6'>Dashboard</h3><div className='space-y-4'><div className='flex justify-between'><span>Users</span><span className='text-2xl font-bold'>{data.dashboard.registeredUsers.toLocaleString()}</span></div><div className='flex justify-between'><span>Projects</span><span className='text-2xl font-bold'>{data.dashboard.projects.toLocaleString()}</span></div></div></div></div></section>;}